# Local working notes for agents and for me. Never committed.
notes/

# Build output and local runtime state
bin/
tmp/
logs/
.env
config.yaml
# The daemon's own defaults when nothing configures it: a local database and a
# local object directory.
goptivum.db
goptivum.db-*
documents/

node_modules/
__pycache__/
*.log

# The bundled contract is versioned for API consumers.
!contracts/dist/

# Real files: personal data, never committed. See tests/README.md
tests/data/

# Stray build output
/plaprobe
